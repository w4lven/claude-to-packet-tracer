# Screenshots

This folder holds screenshots / GIFs referenced from the main `README.md`.

## What to add (high-impact)

To make the project README "wow" visitors, save **two** images here:

1. **`demo-topology.png`** — a screenshot of Packet Tracer showing a
   topology that was generated entirely from a single Claude prompt.
   Good candidate: the lab built in the demo section of the main README
   (2 routers + switch + 2 PCs).
   Where to capture: PT main workspace after opening the generated `.pkt`.

2. *(optional)* **`demo-claude.png`** — a screenshot of the Claude chat
   showing the tool calls happening. Captures the "Claude is editing
   the file" moment.
   Where to capture: Claude Code / Desktop after sending the prompt.

3. *(optional)* **`demo.gif`** — a short screen recording (5–10 sec) of
   the whole flow: prompt → tool calls → reload in PT. Tools like
   ScreenToGif or LICEcap make this easy on Windows / macOS.

Drop the PNG / GIF files in this folder under the exact names listed
above. The main README already references them with the right paths.
